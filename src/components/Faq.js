import React from 'react'
import {SectionText, SectionP} from './Section'

export default () => {
  return <SectionText
    heading={'FAQ'}
    className={'smaller'}>
    <SectionP>Text</SectionP>
  </SectionText>
}